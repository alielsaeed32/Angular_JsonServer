import { CreidtCardPipe } from './creidt-card.pipe';

describe('CreidtCardPipe', () => {
  it('create an instance', () => {
    const pipe = new CreidtCardPipe();
    expect(pipe).toBeTruthy();
  });
});
