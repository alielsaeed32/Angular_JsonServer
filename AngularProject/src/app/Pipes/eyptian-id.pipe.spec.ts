import { EyptianIDPipe } from './eyptian-id.pipe';

describe('EyptianIDPipe', () => {
  it('create an instance', () => {
    const pipe = new EyptianIDPipe();
    expect(pipe).toBeTruthy();
  });
});
